#include "game.h"
#include "vehicle.h"
int main() {
    Vehicle v;
    v.plotTest();

    /*
    Game game;

    while(game.running()) {
        game.update();
        game.render();
    }
    */


    return EXIT_SUCCESS;
}
